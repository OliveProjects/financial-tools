name: Update Mediterranean Data

on:
  schedule:
    - cron: '*/15 8-17 * * 1-5'
  workflow_dispatch:
  push:
    branches: [main]
    paths: ['scripts/fetch_med.py']

permissions:
  contents: write

jobs:
  fetch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install yfinance requests

      - name: Fetch Mediterranean data
        run: python scripts/fetch_med.py

      - name: Commit if changed
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          cp data/med.json /tmp/med_fresh.json
          git checkout -- data/med.json
          git pull --rebase origin main
          cp /tmp/med_fresh.json data/med.json
          git add data/med.json
          git diff --cached --quiet && echo "No changes" || (
            git commit -m "chore: update med data [skip ci]" &&
            git push
          )
